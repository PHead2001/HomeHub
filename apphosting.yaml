# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

  # OpenAI calls run only at request time. The secret value is managed outside Git.
env:
  - variable: OPENAI_API_KEY
    secret: openaiApiKey

  - variable: OPENAI_DEFAULT_MODEL
    value: gpt-5.6-luna
    availability:
      - RUNTIME

  - variable: OPENAI_CATEGORIZATION_MODEL
    value: gpt-5.6-luna
    availability:
      - RUNTIME

  - variable: OPENAI_RECIPE_MODEL
    value: gpt-5.6-terra
    availability:
      - RUNTIME

  - variable: OPENAI_MAINTENANCE_MODEL
    value: gpt-5.6-luna
    availability:
      - RUNTIME
